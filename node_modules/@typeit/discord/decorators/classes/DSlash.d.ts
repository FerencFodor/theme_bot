import { ApplicationCommandData, ApplicationCommandPermissionData, CommandInteraction, CommandInteractionOption } from "discord.js";
import { DOption, PermissionType } from "../..";
import { Method } from "./Method";
export declare class DSlash extends Method {
    private _description;
    private _name;
    private _defaultPermission;
    private _options;
    private _permissions;
    private _guilds;
    private _group;
    private _subgroup;
    get group(): string;
    set group(value: string);
    get subgroup(): string;
    set subgroup(value: string);
    get permissions(): {
        id: string;
        type: PermissionType;
    }[];
    set permissions(value: {
        id: string;
        type: PermissionType;
    }[]);
    get guilds(): string[];
    set guilds(value: string[]);
    get defaultPermission(): boolean;
    set defaultPermission(value: boolean);
    get name(): string;
    set name(value: string);
    get description(): string;
    set description(value: string);
    get options(): DOption[];
    set options(value: DOption[]);
    protected constructor();
    static create(name: string, description?: string, defaultPermission?: boolean, guilds?: string[]): DSlash;
    toSubCommand(): DOption;
    toObject(): ApplicationCommandData;
    getPermissions(): ApplicationCommandPermissionData[];
    getLastNestedOption(options: Map<string, CommandInteractionOption>): CommandInteractionOption[];
    parseParams(interaction: CommandInteraction): (string | number | boolean)[];
}

import { Decorator } from "./Decorator";
import { DGuard, DSlash, DOn } from "../..";
import { PermissionType } from "../../types";
export declare class DDiscord extends Decorator {
    private _guards;
    private _slashes;
    private _events;
    private _description;
    private _name;
    private _defaultPermission;
    private _permissions;
    private _guilds;
    get permissions(): {
        id: string;
        type: PermissionType;
    }[];
    set permissions(value: {
        id: string;
        type: PermissionType;
    }[]);
    get guilds(): string[];
    set guilds(value: string[]);
    get defaultPermission(): boolean;
    set defaultPermission(value: boolean);
    get description(): string;
    set description(value: string);
    get name(): string;
    set name(value: string);
    get guards(): DGuard[];
    set guards(value: DGuard[]);
    get slashes(): DSlash[];
    set slashes(value: DSlash[]);
    get events(): DOn[];
    set events(value: DOn[]);
    get instance(): any;
    protected constructor();
    static create(name: string): DDiscord;
}

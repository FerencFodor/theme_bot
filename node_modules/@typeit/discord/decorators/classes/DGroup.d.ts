import { Decorator } from "./Decorator";
export declare class DGroup<InfoType = any> extends Decorator {
    name: string;
    infos: Partial<InfoType>;
    protected constructor();
    static create<InfoType = any>(name: string, infos?: Partial<InfoType>): DGroup<InfoType>;
}
